/**
 * Constants to declare the expected nominal zIndex for all types of layer in chart rendering.
 */
export declare enum Layers {
    SERIES_BACKGROUND_ZINDEX = 0,
    AXIS_GRID_ZINDEX = 1,
    AXIS_ZINDEX = 2,
    SERIES_CROSSLINE_RANGE_ZINDEX = 3,
    SERIES_LAYER_ZINDEX = 4,
    SERIES_HIGHLIGHT_ZINDEX = 5,
    AXIS_FOREGROUND_ZINDEX = 6,
    SERIES_CROSSHAIR_ZINDEX = 7,
    SERIES_LABEL_ZINDEX = 8,
    SERIES_CROSSLINE_LINE_ZINDEX = 9,
    SERIES_ANNOTATION_ZINDEX = 10,
    LEGEND_ZINDEX = 11,
    NAVIGATOR_ZINDEX = 12
}
