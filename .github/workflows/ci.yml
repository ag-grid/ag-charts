name: CI

on:
    push:
        branches: [latest]
    pull_request:
        branches: [latest]

concurrency:
    group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
    cancel-in-progress: true

jobs:
    execute_blt:
        runs-on: ubuntu-latest
        steps:
            - id: checkout
              uses: actions/checkout@v3
              with:
                  fetch-depth: 0
            - id: nx_affected_setup
              uses: nrwl/nx-set-shas@v3
              with:
                  main-branch-name: 'latest'
            - id: setup_node
              uses: actions/setup-node@v3
              with:
                  node-version: '18'
                  cache: 'yarn'
            - id: yarn_install
              run: yarn install --ci
            - id: format
              run: yarn nx format:check
              continue-on-error: true
            - id: lint
              run: yarn nx affected -t lint --parallel=3
              continue-on-error: true
            - id: build
              run: yarn nx affected -t build --parallel=3
              continue-on-error: true
            - id: test
              if: steps.build.outcome == 'success'
              run: yarn nx affected -t test --configuration=ci
              continue-on-error: true
            - id: results
              run: |
                  echo ${{ steps.format.outcome }} > format.status
                  echo ${{ steps.lint.outcome }} > lint.status
                  echo ${{ steps.build.outcome }} > build.status
                  echo ${{ steps.test.outcome }} > test.status
            - uses: actions/upload-artifact@v3
              with:
                  name: statuses
                  path: |
                      *.status

    format:
        runs-on: ubuntu-latest
        needs: execute_blt
        steps:
            - uses: actions/download-artifact@v3
            - run: if [[ $(cat format.status) != 'success' ]] ; exit 1

    lint:
        runs-on: ubuntu-latest
        needs: execute_blt
        steps:
            - uses: actions/download-artifact@v3
            - run: if [[ $(cat lint.status) != 'success' ]] ; exit 1

    build:
        runs-on: ubuntu-latest
        needs: execute_blt
        steps:
            - uses: actions/download-artifact@v3
            - run: if [[ $(cat build.status) != 'success' ]] ; exit 1

    test:
        runs-on: ubuntu-latest
        needs: execute_blt
        steps:
            - uses: actions/download-artifact@v3
            - run: if [[ $(cat test.status) != 'success' ]] ; exit 1
