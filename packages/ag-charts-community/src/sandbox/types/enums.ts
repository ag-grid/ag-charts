export enum ChartType {
    Cartesian,
    Polar,
    Hierarchy,
    Topology,
}

export enum Position {
    Top = 'top',
    Right = 'right',
    Bottom = 'bottom',
    Left = 'left',
}

export enum Direction {
    Horizontal = 'horizontal',
    Vertical = 'vertical',
}
