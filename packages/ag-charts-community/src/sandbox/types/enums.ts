export enum ChartType {
    Cartesian,
    Polar,
    Hierarchy,
    Topology,
}

export enum Direction {
    Top = 'top',
    Right = 'right',
    Bottom = 'bottom',
    Left = 'left',
}

export enum DiagonalDirection {
    TopRight = 'top-right',
    BottomRight = 'bottom-right',
    BottomLeft = 'bottom-left',
    TopLeft = 'top-left',
}

export enum ExtendedDirection {
    Top = 'top',
    TopRight = 'top-right',
    Right = 'right',
    BottomRight = 'bottom-right',
    Bottom = 'bottom',
    BottomLeft = 'bottom-left',
    Left = 'left',
    TopLeft = 'top-left',
}

export enum DirectionType {
    Horizontal = 'horizontal',
    Vertical = 'vertical',
}
