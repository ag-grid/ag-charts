{
  "name": "all",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/all/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:noop",
      "dependsOn": ["^build"]
    },
    "build-tsc": {
      "executor": "nx:noop",
      "dependsOn": ["^build-tsc"]
    },
    "lint": {
      "executor": "nx:noop",
      "dependsOn": ["^lint"],
      "configurations": {
        "fix": {}
      }
    },
    "test": {
      "executor": "nx:noop",
      "dependsOn": ["^test"],
      "configurations": {
        "update": {}
      }
    },
    "pack": {
      "executor": "nx:noop",
      "dependsOn": ["^pack"],
      "configurations": {
        "production": {}
      }
    },
    "pack:extract": {
      "executor": "nx:noop",
      "dependsOn": ["^pack:extract"],
      "configurations": {
        "production": {}
      }
    },
    "pack:verify": {
      "executor": "nx:noop",
      "dependsOn": ["^pack:verify"],
      "configurations": {
        "production": {}
      }
    },
    "dev:setup": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx build ag-charts-website:generate-examples --batch",
          "nx build ag-charts-website:generate-thumbnails --batch"
        ]
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "dependsOn": ["dev:setup"],
      "options": {
        "parallel": true,
        "commands": [
          "while true ; do nx watch -d -p ag-charts-community,ag-charts-enterprise -- nx run ag-charts-enterprise:build:package --configuration watch ; done",
          "while true ; do nx watch -d -p ag-charts-community,ag-charts-enterprise -- nx run ag-charts-enterprise:build:umd --configuration watch ; done",
          "while true ; do nx watch -d -p ag-charts-community -- nx run ag-charts-community:build:umd --configuration watch ; done",
          "while true ; do nx watch -d -p ag-charts-community -- nx run ag-charts-community:docs-resolved-interfaces ; done",
          "while true ; do nx watch -p ag-charts-website-* -- nx build \\${NX_PROJECT_NAME}:generate; done",
          "nx run ag-charts-website:dev --configuration watch"
        ]
      },
      "configurations": {
        "windows": {
          "commands": [
            {
              "command": "nx run ag-charts-website:dev --configuration watch"
            }
          ]
        }
      }
    },
    "blt": {
      "executor": "nx:run-commands",
      "dependsOn": ["build", "lint", "format-check"],
      "options": {
        "commands": ["nx run-many -t test --parallel 1"]
      }
    },
    "clean": { "command": "rimraf dist/ 'packages/*/dist/'" }
  },
  "implicitDependencies": [
    "ag-charts-community",
    "ag-charts-enterprise",
    "ag-charts-community-examples",
    "ag-charts-website",
    "ag-charts-website-e2e",
    "ag-charts-angular",
    "ag-charts-react",
    "ag-charts-vue",
    "ag-charts-vue3",
    "ag-charts-build-tools",
    "ag-charts-test"
  ],
  "tags": []
}
