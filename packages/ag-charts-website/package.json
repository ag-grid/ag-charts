{
  "name": "ag-charts-website",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npx playwright install chromium && npm run generate-gallery-thumbnails",
    "generate-gallery-thumbnails": "tsx ./scripts/generateGalleryThumbnails.ts"
  }
}
