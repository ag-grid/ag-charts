{
  "name": "ag-charts-website",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "packages/ag-charts-website/src",
  "targets": {
    "build": {
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "dependsOn": ["^build", "^docs-resolved-interfaces", "generate-examples", "generate-thumbnails"],
      "command": "cd packages/ag-charts-website && astro build",
      "options": {
        "silent": true
      },
      "configurations": {
        "staging": {},
        "production": {}
      }
    },
    "build-tsc": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project packages/ag-charts-website/tsconfig.json"
      }
    },
    "dev": {
      "command": "cd packages/ag-charts-website && astro dev --port=4600 --host",
      "dependsOn": ["^build", "^docs-resolved-interfaces"],
      "configurations": {
        "watch": {}
      }
    },
    "preview": {
      "dependsOn": [
        {
          "target": "build"
        }
      ],
      "command": "cd packages/ag-charts-website && astro preview --port=4601",
      "configurations": {
        "staging": {},
        "production": {}
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{workspaceRoot}/reports/ag-charts-website.xml"],
      "dependsOn": [],
      "options": {
        "config": "packages/ag-charts-website/vitest.config.mjs"
      },
      "configurations": {
        "ci": {
          "outputFile": "../../reports/ag-charts-website.xml",
          "reporters": ["junit"]
        },
        "watch": {
          "watch": true
        },
        "update": {
          "update": true
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/ag-charts-website/**/*.ts"],
        "eslintConfig": "packages/ag-charts-website/.eslintrc.json"
      },
      "configurations": {
        "fix": {
          "fix": true
        }
      }
    }
  },
  "implicitDependencies": [
    "ag-charts-community",
    "ag-charts-enterprise",
    "ag-charts-angular",
    "ag-charts-react",
    "ag-charts-vue",
    "ag-charts-vue3"
  ],
  "tags": []
}
