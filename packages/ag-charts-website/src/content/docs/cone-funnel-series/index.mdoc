---
title: 'Cone Funnel Series'
description: '$framework Cone Funnel Series shows the change of a value over a set of processes.'
---

A Funnel Series illustrates how a value changes during a process, with a line representing the value at each stage
and filled areas denoting the change between them.

## Simple Cone Funnel

{% chartExampleRunner title="Cone Funnel Chart" name="simple-cone-funnel" type="generated" /%}

To create a Cone Funnel Series, use the `cone-funnel` series type.

```js format="snippet"
{
    series: [
        {
            type: 'cone-funnel',
            stageKey: 'group',
            valueKey: 'value',
        },
    ],
}
```

In this configuration:

- `stageKey` defines the stages which are used for the lines of the cone funnel.
- `valueKey` provides the numerical values which determine the width of line.

## Horizontal Cone Funnel

{% chartExampleRunner title="Horizontal Cone Funnel Chart" name="horizontal-cone-funnel" type="generated" /%}

To create a horizontal Cone Funnel Series, set the `direction` to `horizontal`.

```js format="snippet"
{
    direction: 'horizontal',
}
```

### Customisation

{% chartExampleRunner title="Cone Funnel Fills" name="cone-funnel-fills" type="generated" /%}

```js format="snippet"
{
    fills: ['#5090DC', '#FFA03A', '#459D55'],
}
```

In this configuration:

- The fills for each drop off area are defined in the `fills` array.
- The Cone Funnel series is reversed by providing the data items in the reverse order.

## Labels

Each divider can show its value with `label`, positioned by the side of the divider it sits on and its position along it.

```js format="snippet"
{
    label: {
        enabled: true,
        placement: 'start-center',
    },
}
```

In this configuration:

- `start` and `end` are the two sides of the divider and `middle` sits on it, while `before`, `center` and `after` position the label along its length.
- `before` and `after` follow text alignment: where the divider spans the horizontal axis, `before` is its left end in a left-to-right chart and its right end in a right-to-left one. Where the divider spans the vertical axis, `before` is always its top end.
- `start` and `end` are direction-aware in the same way: where the divider spans the vertical axis, `start` is its left side in a left-to-right chart and its right side in a right-to-left one.
- The single-word values `'before'`, `'middle'` and `'after'` are deprecated. Use `'start-center'`, `'middle-center'` and `'end-center'` instead.

See [Series Labels](./series-labels/) for the full set of label, placement and collision options.

## API Reference

{% tabs %}

{% tabItem id="AgConeFunnelSeriesOptions" label="Cone Funnel Series" %}
{% apiReference id="AgConeFunnelSeriesOptions" /%}
{% /tabItem %}

{% /tabs %}
