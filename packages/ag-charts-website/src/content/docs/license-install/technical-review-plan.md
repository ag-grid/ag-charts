# Technical Review Plan: License Install Documentation

## Page Analysis Summary

### Features Covered

-   Installing AG Charts Enterprise license key
-   License validation process
-   Setting up application with license
-   Dependencies configuration

### Key Components

-   The page uses a custom `{% licenseSetup /%}` component that dynamically generates content
-   The component is framework-aware (React, Angular, Vue, JavaScript)
-   Main API: `LicenseManager.setLicenseKey(key)`
-   Component location: `packages/ag-charts-website/src/components/license-setup/components/LicenseSetup.tsx`

### Documentation Structure

The page is minimal with headings indicating:

1. Validate Your Licence
2. Add Your Dependencies
3. Set Up Your Application

The actual content is generated by the LicenseSetup component which provides framework-specific instructions.

## Validation Targets

### TypeScript Interfaces to Verify

1. **LicenseManager API**

    - Location: `packages/ag-charts-enterprise/src/main.ts`
    - Exported method: `setLicenseKey(key: string)`
    - Also check: `packages/ag-charts-enterprise/src/license/licenseManager.ts`

2. **IntegratedModule interface**

    - Location: `packages/ag-charts-types/src/integratedCharts.ts`
    - Verify `setLicenseKey` is part of the interface

3. **AgChartsEnterpriseModule**
    - Location: `packages/ag-charts-enterprise/src/main.ts`
    - Verify it includes `setLicenseKey` method

### Implementation Files to Check

1. **License Manager Implementation**

    - `packages/ag-charts-enterprise/src/license/licenseManager.ts`
    - Verify actual validation logic
    - Check error handling for invalid licenses
    - Verify license format requirements

2. **Setup Module**

    - `packages/ag-charts-enterprise/src/setup.ts`
    - Check how enterprise modules are initialized
    - Verify when license validation occurs

3. **LicenseSetup Component**
    - `packages/ag-charts-website/src/components/license-setup/components/LicenseSetup.tsx`
    - Verify generated code examples
    - Check framework-specific implementations
    - Validate dependency instructions

### Examples to Test

Since this page uses a dynamic component instead of static examples, testing will focus on:

1. **Component Output Validation**

    - Navigate to the live page at `https://localhost:4600/charts/javascript/license-install/`
    - Verify the LicenseSetup component renders correctly
    - Check that framework-specific code is generated
    - Test switching between frameworks (if applicable)

2. **Generated Code Validation**

    - Extract and validate code snippets shown for:
        - JavaScript/TypeScript setup
        - React setup (if different)
        - Angular setup (if different)
        - Vue setup (if different)
    - Verify import statements are correct
    - Check that `setLicenseKey` is called correctly

3. **Dependency Instructions**
    - Verify npm/yarn commands shown are accurate
    - Check package names (`ag-charts-enterprise`)
    - Validate version compatibility information

### User Interactions to Validate

1. **Framework Switching** (if available)

    - Test switching between JavaScript, React, Angular, Vue
    - Verify code examples update appropriately
    - Check that dependency instructions change

2. **Code Copy Functionality**

    - Test copy buttons on code snippets
    - Verify copied code is complete and functional

3. **License Key Input** (if interactive)
    - Check if there's a license key input field
    - Test validation feedback for invalid keys
    - Verify error messages are helpful

### Visual States to Screenshot

1. **Default Page Load**

    - Full page view showing all sections
    - Code examples for default framework

2. **Framework Variations**

    - Screenshot each framework's code example
    - Capture any framework-specific instructions

3. **Interactive Elements**
    - Any dropdowns or switchers
    - Copy button states
    - Error states (if accessible)

## Known Exceptions

No documented exceptions file exists for this page.

## Execution Plan

### Priority 1: Core API Validation

1. **Verify LicenseManager API**

    - Check `setLicenseKey` method exists and is exported
    - Validate method signature matches documentation
    - Check for any additional license-related methods

2. **Validate Component Output**
    - Navigate to live page
    - Screenshot default state
    - Extract generated code examples
    - Verify imports and API usage

### Priority 2: Framework-Specific Testing

1. **Test Each Framework Variant**

    - Switch to each framework (if switcher exists)
    - Screenshot framework-specific code
    - Validate framework-specific setup instructions
    - Check dependency instructions match framework

2. **Code Quality Validation**
    - Delegate to example-tester agent with:
        - Generated code snippets from each framework
        - Expected behavior: License key should be set without errors
        - Validation: No TypeScript errors, correct imports

### Priority 3: Implementation Details

1. **License Validation Logic**

    - Review actual license validation implementation
    - Check error handling and messages
    - Verify license format requirements

2. **Enterprise Setup Flow**
    - Understand when license is validated
    - Check module initialization order
    - Verify enterprise features are properly gated

### Priority 4: User Experience

1. **Interactive Testing**

    - Test all clickable elements
    - Verify copy functionality
    - Check responsive behavior

2. **Documentation Completeness**
    - Verify all necessary steps are documented
    - Check for missing information about:
        - License format
        - Error troubleshooting
        - License expiration handling

## Success Criteria

1. **API Accuracy**: `setLicenseKey` method exists and works as documented
2. **Code Examples**: All generated code examples are syntactically correct and functional
3. **Framework Coverage**: Each supported framework has accurate setup instructions
4. **Dependencies**: Package names and versions are correct
5. **User Flow**: A developer can successfully install and validate a license following the documentation

## Estimated Complexity

-   **High**: This page uses dynamic content generation which requires careful validation
-   **Time Estimate**: 45-60 minutes due to multiple framework variants and dynamic content
-   **Special Considerations**:
    -   Must test the live component rather than static examples
    -   Need to validate generated code for multiple frameworks
    -   License validation logic may be complex
