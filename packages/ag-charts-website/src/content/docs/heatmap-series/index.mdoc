---
title: 'Heatmap Series'
description: '$framework Heatmap Charts display data in a matrix format, using colours to denote the magnitude of the values. Customise the colour scale and labels. Gradient legend support.'
enterprise: true
---

A Heatmap Series displays data in a matrix format, using colours to denote the magnitude of the values.

## Simple Heatmap

{% chartExampleRunner title="Simple Heatmap" name="simple-heatmap" type="generated" /%}

To create a Heatmap Series, use the `heatmap` series type.

```js format="snippet"
{
    series: [
        {
            type: 'heatmap',
            xKey: 'month',
            yKey: 'year',
            colorKey: 'temperature',
        },
    ],
}
```

In this configuration:

- `xKey` is set to 'month', which is the category for the x-axis.
- `yKey` is set to 'year', which is the category for the y-axis.
- `colorKey` is set to 'temperature', which supplies numerical values for the Colour Scale.

## Colour Scale

{% chartExampleRunner title="Colour Scale" name="color-range-with-many-values" type="generated" /%}

Use `colorScale` to control how numeric values map to colours. This includes custom colours, discrete bins, and a fixed domain.

```js format="snippet"
{
    series: [
        {
            type: 'heatmap',
            xKey: 'month',
            yKey: 'year',
            colorKey: 'temperature',
            colorScale: {
                fills: [{ color: 'navy' }, { color: 'lightyellow', stop: 10 }, { color: 'darkred' }],
            },
        },
    ],
}
```

In this example:

- `fills` specifies the colours and optional `stop` positions. Without `stop` values, colours are spaced equally across the data range.
- `mode` switches between a `'continuous'` gradient and `'discrete'` bins.
- `domain` overrides the auto-detected range to consistent colours regardless of the data extent.

For the full range of options including named stops, missing data handling, and more see the [Colour Scale](./colour-scale/) page.

## Labels

{% chartExampleRunner title="Heatmap Label" name="customising-labels" type="generated" /%}

If `label.enabled` is set to `true`, the labels will show the numeric value from `colorKey`.

```js format="snippet"
colorKey: 'temperature',
label: {
    enabled: true,
    formatter: ({ datum: { temperature } }) => `${temperature.toFixed(0)}°C`,
}
```

A label `formatter` can be used to customise the label text.

## Gradient Legend

The Gradient Legend is enabled by default for heatmap series using a `colorKey`. It displays a colour bar to help match cell colours to values.

{% chartExampleRunner title="Gradient Legend" name="gradient-legend" type="generated" /%}

```js format="snippet"
{
    gradientLegend: {
        enabled: true,
    },
}
```

For position, size, and label customisation options, see the [Colour Scale](./colour-scale/#gradient-legend) page.

## Heatmap Chart Examples

{% galleryExamples series="heatmap" /%}

## API Reference

{% tabs %}

{% tabItem id="AgHeatmapSeriesOptions" label="Heatmap Series" %}
{% apiReference id="AgHeatmapSeriesOptions" /%}
{% /tabItem %}

{% tabItem id="AgGradientLegendOptions" label="Gradient Legend" %}
{% apiReference id="AgGradientLegendOptions" /%}
{% /tabItem %}

{% /tabs %}
