---
title: 'Series Fills'
---

Series and Markers can have solid, gradient and pattern fills, allowing for unique visual styles and improved contrasts between series.

## Fill Types

{% chartExampleRunner title="Fill Types" name="series-fill-types" type="generated" /%}

Supported Fill Types are:

-   **Solid Fill**: A single CSS colour (provided as a `#rgb` hex code, `rgb()` or `rgba()` values, or a CSS colour name such as 'black').

-   **Gradient Fill**: A transition between multiple colours.

-   **Pattern Fill**: Predefined patterns including lines and shapes.

-   **Image Fill**: An image.

The default colours and fills come from the default or user provided [Theme Palette](./themes/#palette).

## Setting a Fill

### Series

The `fill` attribute is set at the series level.

Solid fills use a CSS colour string. Gradient and Pattern fills require an object with type `gradient` or `pattern`.

```js
series: [
    // ...
    fill: {
        type: 'gradient'
    }
]
```

### Markers

[Series Markers](./markers) support the same fill options. The `fill` attribute is contained in the `marker` property of the `series` options.

```js
series: [
    // ...
    marker: {
        fill: {
            type: 'gradient'
        },
    },
]
```

## Customisation

## Gradients

{% chartExampleRunner title="Gradient Fill" name="gradient-fill" type="generated" /%}

Gradient `colorStops` define an array of colours for the gradient to use.

The array should contain a minimum of two objects, with each object providing optional values for:

-   `color`: Any valid CSS colour value.
-   `stop`: A ratio between `0` and `1` indicating the position within the gradient where the colour changes.

```js
{
    series: {
        fill: {
            type: 'gradient',
            colorStops: [
                { color: '#70C1FF', stop: 0.1 },
                { color: '#FFD86F', stop: 0.3 },
                { color: '#FF9A60', stop: 0.5},
                { color: '#D16BA5' },  //will continue to the end
            ],
        },
    },
}
```

In this configuration:

-   Each colour stops at the `stop` value, and the next colour begins at that point.
-   If no `stop` is provided, the fills will be distributed equally.
-   The last colour is used until the end of the gradient scale.

See the [Gradient API reference](#reference-AgGradientColor) for all the available options.

## Patterns

### Predefined

{% chartExampleRunner title="Pattern Fill" name="pattern-fill" type="generated" /%}

Any of the [stock patterns](#reference-AgPatternColor-pattern) can be provided in the `fill.pattern` property.

```js
{
    series: {
        fill: {
            type: 'pattern',
            pattern: 'stars'
        },
    },
}
```

Stock patterns include:

-   Lines: `vertical-lines`, `horizontal-lines`, `forward-slanted-lines`, `backward-slanted-lines`.
-   Shapes: `squares`, `circles`, `triangles`, `diamonds`, `stars`, `hearts`, `crosses`.

{% chartExampleRunner title="Pattern Fill Customisation" name="pattern-fill-customisation" type="generated" /%}

Use styling properties such as `stroke`, `fill` and `backgroundFill` to further customise the patterns.

### Path

SVG Path Data can be used to create custom patterns.

```html
<svg viewBox="0 0 16 10">
    <path d="M0,6 Q4,1 8,6 T16,6" />
</svg>
```

-   In the snippet above, the value of the `d` attribute on the SVG <path> element is `M4,6 Q8,1 12,6 T20,6`.
-   It defines the shape of the path and can be used as a string in the `fill.path` property:

```js
{
    series: {
        fill: {
            type: 'pattern',
            path: 'M0,6 Q4,1 8,6 T16,6' // svg path data string
        },
    },
}
```

{% chartExampleRunner title="Pattern Custom Path" name="pattern-custom-path" type="generated" /%}

For detailed SVG Path Data syntax, refer to the [SVG Path Specification](https://www.w3.org/TR/SVG/paths.html#TheDProperty).

A path consists of multiple segments defined by various commands. Each command (except `moveto` and `closepath`) creates a new path segment.
All coordinates and lengths in the path data are interpreted in user units relative to the current coordinate system.

To be valid, the path string must follow the SVG path grammar outlined in the [Path Data BNF](https://www.w3.org/TR/SVG/paths.html#PathDataBNF).

Line Drawing Commands:

-   L, l — Lineto (absolute and relative)
-   H, h — Horizontal lineto
-   V, v — Vertical lineto
-   Z, z — Close path

Cubic Bézier Curve Drawing Commands:

-   C, c — Specify two control points and an endpoint
-   S, s — Smooth cubic Bézier, uses the reflection of the previous control point

Quadratic Bézier Curve Drawing Commands:

-   Q, q — Specify one control point and an endpoint
-   T, t — Smooth quadratic Bézier, uses the reflection of the previous control point

Elliptical Arc Drawing Commands:

-   A, a — Draws an elliptical arc segment

## Images

### Image Cover
{% chartExampleRunner title="Image Fill" name="image-fill" type="generated" /%}

Any image url or data:url can be provided in the `fill.image.url` property.

```js
{
    series: {
        fill: {
            type: 'image',
            url: 'url'
            fit: 'cover'
        },
    },
}
```

### Image Repeat

{% chartExampleRunner title="Image Fill Repeat" name="image-fill-repeat" type="generated" /%}
```js
{
    series: {
        fill: {
            type: 'image',
            url: 'url',
            repetition: 'repeat'
         },
    },
}
```

See the [Image API reference](#reference-AgImageColor) all the available options.

## API Reference

{% tabs %}

{% tabItem id="AgGradientColor" label="Gradient Fill" %}
{% apiReference id="AgGradientColor" /%}
{% /tabItem %}

{% tabItem id="AgPatternColor" label="Pattern Fill" %}
{% apiReference id="AgPatternColor" /%}
{% /tabItem %}

{% tabItem id="AgImageFill" label="Image Fill" %}
{% apiReference id="AgImageFill" /%}
{% /tabItem %}

{% /tabs %}
