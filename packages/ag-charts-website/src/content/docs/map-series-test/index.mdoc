---
title: 'Map Series Test'
---

## Routing

What to test:

- `map-shape`, `map-line` and `map-marker` layers render together with the routes drawn over the shapes and city markers on top.
- Toggle legend items; each layer hides and restores independently.

{% chartExampleRunner title="Routing" name="routing" type="generated" /%}

## Floating Gradient Legend

What to test:

- The gradient legend floats inside the plot area at the configured offset and does not reflow the map.
- Resize the window; the legend keeps its placement relative to the bottom-left corner.

{% chartExampleRunner title="Floating Gradient Legend" name="heatmap-floating" type="generated" /%}
