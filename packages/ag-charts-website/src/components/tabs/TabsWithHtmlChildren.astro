---
import { TabsWithData } from './TabsWithData';
import { extractTabsData } from './utils';

const html = await Astro.slots.render('default');
const tabsData = extractTabsData(html);

const { tabItemIdPrefix } = Astro.props;
---

<TabsWithData client:load tabsData={tabsData} tabItemIdPrefix={tabItemIdPrefix} />
