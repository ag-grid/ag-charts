import { _Theme } from 'ag-charts-community';

export const ERROR_BARS_THEME = {
    errorBar: {
        visible: true,
        stroke: 'black',
        strokeWidth: 1,
        strokeOpacity: 1,
        cap: {
            length: undefined,
            lengthRatio: undefined,
        },
    },
};
