{
    "AgBaseChartListeners.activeChange": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.annotations": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.axisClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.axisDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.captionClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.captionDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.collapsedChange": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.click": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.doubleClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.selectionChange": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.seriesNodeClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.seriesNodeDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.seriesVisibilityChange": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.zoom": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgChartLegendListeners.legendItemClick": {
        "classification": "zone-wrap",
        "reason": "Legend listener; re-entered into the zone by patchChartOptions via patchListeners(options.legend.listeners)."
    },
    "AgChartLegendListeners.legendItemDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Legend listener; re-entered into the zone by patchChartOptions via patchListeners(options.legend.listeners)."
    },
    "AgSeriesListeners.seriesNodeClick": {
        "classification": "zone-wrap",
        "reason": "Series listener; re-entered into the zone by patchChartOptions via patchListeners(series.listeners)."
    },
    "AgSeriesListeners.seriesNodeDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Series listener; re-entered into the zone by patchChartOptions via patchListeners(series.listeners)."
    },
    "AgHistogramSeriesListeners.seriesNodeClick": {
        "classification": "zone-wrap",
        "reason": "Series listener; re-entered into the zone by patchChartOptions via patchListeners(series.listeners)."
    },
    "AgHistogramSeriesListeners.seriesNodeDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Series listener; re-entered into the zone by patchChartOptions via patchListeners(series.listeners)."
    },
    "AgAxisListeners.click": {
        "classification": "zone-wrap",
        "reason": "Axis listener; re-entered into the zone by patchChartOptions via patchListeners(axes[<key>].listeners)."
    },
    "AgAxisListeners.doubleClick": {
        "classification": "zone-wrap",
        "reason": "Axis listener; re-entered into the zone by patchChartOptions via patchListeners(axes[<key>].listeners)."
    },
    "AgCaptionListeners.click": {
        "classification": "zone-wrap",
        "reason": "Caption listener; re-entered into the zone by patchChartOptions via patchListeners(title|subtitle|footnote.listeners)."
    },
    "AgCaptionListeners.doubleClick": {
        "classification": "zone-wrap",
        "reason": "Caption listener; re-entered into the zone by patchChartOptions via patchListeners(title|subtitle|footnote.listeners)."
    },
    "AgContextMenuOptions.getItems": {
        "classification": "zone-wrap",
        "reason": "Returns menu items carrying action callbacks; patchChartOptions wraps the returned items' actions via patchContextMenu."
    },
    "AgContextMenuItemAlways.action": {
        "classification": "zone-wrap",
        "reason": "Context-menu action dispatched from out-of-zone DOM listeners; re-entered into the zone by patchContextMenu."
    },
    "AgContextMenuItemAxis.action": {
        "classification": "zone-wrap",
        "reason": "Context-menu action dispatched from out-of-zone DOM listeners; re-entered into the zone by patchContextMenu."
    },
    "AgContextMenuItemCaption.action": {
        "classification": "zone-wrap",
        "reason": "Context-menu action dispatched from out-of-zone DOM listeners; re-entered into the zone by patchContextMenu."
    },
    "AgContextMenuItemCrossLine.action": {
        "classification": "zone-wrap",
        "reason": "Context-menu action dispatched from out-of-zone DOM listeners; re-entered into the zone by patchContextMenu."
    },
    "AgContextMenuItemSeriesArea.action": {
        "classification": "zone-wrap",
        "reason": "Context-menu action dispatched from out-of-zone DOM listeners; re-entered into the zone by patchContextMenu."
    },
    "AgContextMenuItemSeriesNode.action": {
        "classification": "zone-wrap",
        "reason": "Context-menu action dispatched from out-of-zone DOM listeners; re-entered into the zone by patchContextMenu."
    },
    "AgContextMenuItemLegendItem.action": {
        "classification": "zone-wrap",
        "reason": "Context-menu action dispatched from out-of-zone DOM listeners; re-entered into the zone by patchContextMenu."
    },
    "AgAxisListeners.crossLineClick": {
        "classification": "zone-wrap",
        "reason": "Axis listener; re-entered into the zone by patchChartOptions via patchListeners(axes[<key>].listeners)."
    },
    "AgAxisListeners.crossLineDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Axis listener; re-entered into the zone by patchChartOptions via patchListeners(axes[<key>].listeners)."
    },
    "AgBaseChartListeners.crossLineClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgBaseChartListeners.crossLineDoubleClick": {
        "classification": "zone-wrap",
        "reason": "Chart listener; re-entered into the zone by patchChartOptions via patchListeners(options.listeners)."
    },
    "AgChartValidationsOptions.issueRaised": {
        "classification": "zone-wrap",
        "reason": "Validation issues are dispatched while options are applied, which the wrapper does outside the zone; re-entered into the zone by patchChartOptions."
    },
    "AgCrossLineListeners.click": {
        "classification": "zone-wrap",
        "reason": "Cross-line listener; re-entered into the zone by patchChartOptions via patchListeners(axes[<key>].crossLines[].listeners)."
    },
    "AgCrossLineListeners.doubleClick": {
        "classification": "zone-wrap",
        "reason": "Cross-line listener; re-entered into the zone by patchChartOptions via patchListeners(axes[<key>].crossLines[].listeners)."
    }
}
