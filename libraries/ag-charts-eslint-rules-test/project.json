{
  "name": "ag-charts-eslint-rules-test",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libraries/ag-charts-eslint-rules-test/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "nx:noop",
      "dependsOn": ["lint:eslint"],
      "configurations": {
        "fix": {}
      }
    },
    "lint:eslint": {},
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libraries/ag-charts-eslint-rules-test/jest.config.ts"
      }
    }
  },
  "tags": ["support"]
}
